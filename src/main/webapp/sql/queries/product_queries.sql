-- =====================================================================
-- 상품(bs_product) 개발/테스트용 쿼리 모음 + 공통 확인용
-- ※ DB 세팅할 때 실행하는 파일 아님. 필요한 줄만 골라서 실행
-- =====================================================================

-- 테이블 구조 확인
desc bs_product;

--테이블 데이터 확인
select * from bs_product;
SELECT * from bs_product WHERE p_id='P1252';

-- 파일명 컬럼 크기 변경 (필요할 때만)
-- ALTER TABLE bs_product MODIFY p_filename VARCHAR2(200);

-- ---------------------------------------------------------------------
-- 공통
-- ---------------------------------------------------------------------
-- 테이블 목록
select * from tab;
--휴지통 비우기 (DROP TABLE 후 BIN$... 테이블이 남아 있을 때)
purge recyclebin;
